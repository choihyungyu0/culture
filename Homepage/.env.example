# 이 파일을 복사해 .env.local 을 만들고 값을 채우세요. (.env.local 은 git에 커밋되지 않음)
# 서버 전용 키 — NEXT_PUBLIC_ 접두사를 붙이지 마세요(클라이언트 노출 금지).
# 향후 검색 / AI 문의 기능에서 서버 측 Route Handler(app/api/.../route.ts)에서만 사용합니다.

OPENAI_API_KEY=
